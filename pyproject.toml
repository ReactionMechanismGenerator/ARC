[build-system]
requires = [
    "setuptools>=64",
    "wheel",
    "Cython",
    "numpy",
]
build-backend = "setuptools.build_meta"

[project]
name = "ARC"
dynamic = ["version"]
requires-python = ">=3.14"
