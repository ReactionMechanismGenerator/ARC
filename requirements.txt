# Required python packages. You can install them all via something like:
#    pip install -r requirements.txt

paramiko==2.4.2
matplotlib==2.2.2
numpy==1.11.3
cclib==1.6
py3Dmol==0.8.0

# required for rmg until new binaries are released
tensorflow

# For running the unit tests
nose
coverage
